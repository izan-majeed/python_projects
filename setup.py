from setuptools import setup

setup(
    name = 'keyword-helper',
    version = '0.0.1',
    description = "A simple python keyword helper which depends on 'tkinter' package",
    py_modules = ['keyword_helper',],
    package_dir = {'':'src'}
)